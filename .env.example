# 示例环境变量文件。不要在仓库中存放真实密钥。
# 将真实 key 放在你的 shell 或 CI 环境变量中。

# OpenAI API key (standard OpenAI SDK)
OPENAI_API_KEY=

# OpenRouter API key（可选，用于 openrouter.ai）
OPENROUTER_API_KEY=

# 可选：覆盖默认模型
OPENAI_MODEL=gpt-4.1-mini
